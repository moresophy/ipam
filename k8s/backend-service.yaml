apiVersion: v1
kind: Service
metadata:
  name: ipam-backend
  namespace: ipam
  labels:
    app: ipam-backend
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: ipam-backend
