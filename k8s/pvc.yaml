apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ipam-data
  namespace: ipam
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Uncomment and adjust if you have a specific storage class
  # storageClassName: standard
